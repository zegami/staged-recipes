package:
  name: motor
  version: "1.1"

source:
  fn: motor-1.1.tar.gz
  url: https://pypi.python.org/packages/de/82/851117322e15f5695aa7a5cc2589a41496548ec281e335732dbeff7f9c5c/motor-1.1.tar.gz
  md5: a07f26422c6b7e6e85e384de8ef07e29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motor = motor:main
    #
    # Would create an entry point called motor that calls motor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=3.4,<4

  run:
    - python
    - pymongo >=3.4,<4

test:
  # Python imports
  imports:
    - motor
    #- motor.frameworks
    #- motor.frameworks.asyncio
    #- motor.frameworks.tornado

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.


about:
  home: https://github.com/mongodb/motor/
  license: Apache Software License
  summary: 'Non-blocking MongoDB driver for Tornado or asyncio'
  license_family: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
